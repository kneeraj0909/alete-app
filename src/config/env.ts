import {
  X_META_SESSION,
  LOGIN_ENDPOINT,
  PORTFOLIO_ENDPOINT
} from '@env';

const config = {
  xMetaSession: X_META_SESSION,
  loginEndPoint: LOGIN_ENDPOINT,
  portfolioEndPoint: PORTFOLIO_ENDPOINT,
};

export default config;
